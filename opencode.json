{
  "version": "2.0",
  "name": "RuneReader Voice",
  "ignore": [
    "**/bin/**",
    "**/obj/**",
    "**/node_modules/**",
    ".git/**"
  ],
  "agents": {
    "default": {
      "model": "default",
      "system_prompt": "You are working on RuneReader Voice, a C# Avalonia desktop companion for AI-generated WoW voice acting. Server/ and LUAAddon/ are sibling projects stored here for reference — do not modify them unless explicitly asked. Reference design docs live in RRV Notes/ and the barcode font spec is at BarcodeFont/RuneReaderBarcode-Spec.md. Read AGENTS.md and PROJECT_RULES.md first for project context and working conventions.",
      "temperature": 0.3
    }
  },
  "commands": {
    "build": "dotnet build RuneReaderVoice.sln",
    "test": "dotnet build RuneReaderVoice.sln"
  },
  "customCommands": [
    {
      "name": "start-server",
      "command": "cd Server/rrv-server && uvicorn server.main:app",
      "description": "Start the TTS server (deps must be installed already)"
    }
  ]
}